# ============================================================
# Dependencias del proyecto — Monitor Loreto IA
# ============================================================

# --- Frontend ---
streamlit
folium
streamlit-folium
pyproj

# --- Geoespacial (sintetizador local / scripts ETL) ---
geopandas
shapely

# NOTA: La lectura de GCS se hace via gsutil (gcloud SDK ya instalado),
#       por lo que NO se necesita google-cloud-storage como dependencia.

# --- ML (solo si se usa inferencia local; en producción corre en Colab) ---
# tensorflow
